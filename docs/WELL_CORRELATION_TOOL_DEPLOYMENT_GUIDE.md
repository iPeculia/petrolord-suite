# Deployment Guide

## Steps
1.  **Build**: `npm run build`
2.  **Deploy**: Upload `dist/` to server.
3.  **Verify**: Check URL `/dashboard/geoscience/well-correlation-panel`.

## Rollback
Revert to previous git tag and redeploy artifacts.