# Developer Guide

## Environment Setup
1.  **Node.js**: Ensure Node v20+ is installed.
2.  **Dependencies**: Run `npm install` in the root directory.
3.  **Env Vars**: Create `.env` with Supabase credentials (see `lib/customSupabaseClient.js`).

## Running Locally