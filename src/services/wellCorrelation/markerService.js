/**
 * Marker Service
 * Operations for markers.
 */

export const fetchMarkers = async (projectId) => {
  return [];
};

export const saveMarker = async (marker) => {
  return marker;
};

export const deleteMarker = async (markerId) => {
  return true;
};