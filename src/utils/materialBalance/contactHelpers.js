/**
 * Fluid Contact Movement Helpers
 */

export const calculateContactMovement = (productionHistory, tankGeometry) => {
  // Stub: Calculate GOC/OWC movement based on voidage and geometry
  return {
    GOC: [],
    OWC: []
  };
};

export const validateContactData = (observations) => {
  // Stub: Check for physical consistency (e.g., GOC always above OWC)
  return true;
};